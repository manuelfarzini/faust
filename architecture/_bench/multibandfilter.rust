/* ------------------------------------------------------------
author: "Grame"
copyright: "(c)GRAME 2006"
license: "BSD"
name: "multibandfilter"
version: "1.0"
Code generated with Faust 2.85.5 (https://faust.grame.fr)
Compilation options: -lang rust -fpga-mem-th 4 -ct 1 -es 1 -mcd 16 -mdd 1024 -mdy 33 -double -ftz 0
------------------------------------------------------------ */

#[repr(C)]
pub struct mydsp {
	fSampleRate: i32,
	fConst0: F64,
	fEntry0: FaustFloat,
	fVslider0: FaustFloat,
	fEntry1: FaustFloat,
	fEntry2: FaustFloat,
	fVslider1: FaustFloat,
	fEntry3: FaustFloat,
	fEntry4: FaustFloat,
	fVslider2: FaustFloat,
	fEntry5: FaustFloat,
	fEntry6: FaustFloat,
	fVslider3: FaustFloat,
	fEntry7: FaustFloat,
	fEntry8: FaustFloat,
	fVslider4: FaustFloat,
	fEntry9: FaustFloat,
	fEntry10: FaustFloat,
	fVslider5: FaustFloat,
	fEntry11: FaustFloat,
	fEntry12: FaustFloat,
	fVslider6: FaustFloat,
	fEntry13: FaustFloat,
	fEntry14: FaustFloat,
	fVslider7: FaustFloat,
	fEntry15: FaustFloat,
	fEntry16: FaustFloat,
	fVslider8: FaustFloat,
	fEntry17: FaustFloat,
	fEntry18: FaustFloat,
	fVslider9: FaustFloat,
	fEntry19: FaustFloat,
	fRec9: [F64;3],
	fRec8: [F64;3],
	fRec7: [F64;3],
	fRec6: [F64;3],
	fRec5: [F64;3],
	fRec4: [F64;3],
	fRec3: [F64;3],
	fRec2: [F64;3],
	fRec1: [F64;3],
	fRec0: [F64;3],
}


fn mydsp_faustpower2_f(value: F64) -> F64 {
	return value * value;
}
pub const FAUST_INPUTS: usize = 1;
pub const FAUST_OUTPUTS: usize = 1;
pub const FAUST_ACTIVES: usize = 30;
pub const FAUST_PASSIVES: usize = 0;

impl mydsp {
		
	pub fn new() -> mydsp { 
		mydsp {
			fSampleRate: 0,
			fConst0: 0.0,
			fEntry0: 0.0,
			fVslider0: 0.0,
			fEntry1: 0.0,
			fEntry2: 0.0,
			fVslider1: 0.0,
			fEntry3: 0.0,
			fEntry4: 0.0,
			fVslider2: 0.0,
			fEntry5: 0.0,
			fEntry6: 0.0,
			fVslider3: 0.0,
			fEntry7: 0.0,
			fEntry8: 0.0,
			fVslider4: 0.0,
			fEntry9: 0.0,
			fEntry10: 0.0,
			fVslider5: 0.0,
			fEntry11: 0.0,
			fEntry12: 0.0,
			fVslider6: 0.0,
			fEntry13: 0.0,
			fEntry14: 0.0,
			fVslider7: 0.0,
			fEntry15: 0.0,
			fEntry16: 0.0,
			fVslider8: 0.0,
			fEntry17: 0.0,
			fEntry18: 0.0,
			fVslider9: 0.0,
			fEntry19: 0.0,
			fRec9: [0.0;3],
			fRec8: [0.0;3],
			fRec7: [0.0;3],
			fRec6: [0.0;3],
			fRec5: [0.0;3],
			fRec4: [0.0;3],
			fRec3: [0.0;3],
			fRec2: [0.0;3],
			fRec1: [0.0;3],
			fRec0: [0.0;3],
		}
	}
	pub fn metadata(&self, m: &mut dyn Meta) { 
		m.declare("author", r"Grame");
		m.declare("bandfilter.dsp/author", r"Grame");
		m.declare("bandfilter.dsp/copyright", r"(c)GRAME 2006");
		m.declare("bandfilter.dsp/license", r"BSD");
		m.declare("bandfilter.dsp/name", r"bandfilter");
		m.declare("bandfilter.dsp/version", r"1.0");
		m.declare("compile_options", r"-lang rust -fpga-mem-th 4 -ct 1 -es 1 -mcd 16 -mdd 1024 -mdy 33 -double -ftz 0");
		m.declare("copyright", r"(c)GRAME 2006");
		m.declare("filename", r"multibandfilter.dsp");
		m.declare("license", r"BSD");
		m.declare("math.lib/author", r"GRAME");
		m.declare("math.lib/copyright", r"GRAME");
		m.declare("math.lib/deprecated", r"This library is deprecated and is not maintained anymore. It will be removed in August 2017.");
		m.declare("math.lib/license", r"LGPL with exception");
		m.declare("math.lib/name", r"Math Library");
		m.declare("math.lib/version", r"1.0");
		m.declare("music.lib/author", r"GRAME");
		m.declare("music.lib/copyright", r"GRAME");
		m.declare("music.lib/deprecated", r"This library is deprecated and is not maintained anymore. It will be removed in August 2017.");
		m.declare("music.lib/license", r"LGPL with exception");
		m.declare("music.lib/name", r"Music Library");
		m.declare("music.lib/version", r"1.0");
		m.declare("name", r"multibandfilter");
		m.declare("version", r"1.0");
	}

	pub fn get_sample_rate(&self) -> i32 { self.fSampleRate as i32}
	
	pub fn class_init(sample_rate: i32) {
		// Obtaining locks on 0 static var(s)
	}
	pub fn instance_reset_params(&mut self) {
		self.fEntry0 = (1e+04) as FaustFloat;
		self.fVslider0 = (0.0) as FaustFloat;
		self.fEntry1 = (5e+01) as FaustFloat;
		self.fEntry2 = (9e+03) as FaustFloat;
		self.fVslider1 = (0.0) as FaustFloat;
		self.fEntry3 = (5e+01) as FaustFloat;
		self.fEntry4 = (8e+03) as FaustFloat;
		self.fVslider2 = (0.0) as FaustFloat;
		self.fEntry5 = (5e+01) as FaustFloat;
		self.fEntry6 = (7e+03) as FaustFloat;
		self.fVslider3 = (0.0) as FaustFloat;
		self.fEntry7 = (5e+01) as FaustFloat;
		self.fEntry8 = (6e+03) as FaustFloat;
		self.fVslider4 = (0.0) as FaustFloat;
		self.fEntry9 = (5e+01) as FaustFloat;
		self.fEntry10 = (5e+03) as FaustFloat;
		self.fVslider5 = (0.0) as FaustFloat;
		self.fEntry11 = (5e+01) as FaustFloat;
		self.fEntry12 = (4e+03) as FaustFloat;
		self.fVslider6 = (0.0) as FaustFloat;
		self.fEntry13 = (5e+01) as FaustFloat;
		self.fEntry14 = (3e+03) as FaustFloat;
		self.fVslider7 = (0.0) as FaustFloat;
		self.fEntry15 = (5e+01) as FaustFloat;
		self.fEntry16 = (2e+03) as FaustFloat;
		self.fVslider8 = (0.0) as FaustFloat;
		self.fEntry17 = (5e+01) as FaustFloat;
		self.fEntry18 = (1e+03) as FaustFloat;
		self.fVslider9 = (0.0) as FaustFloat;
		self.fEntry19 = (5e+01) as FaustFloat;
	}
	pub fn instance_clear(&mut self) {
		for l0 in 0..3 {
			self.fRec9[l0 as usize] = 0.0;
		}
		for l1 in 0..3 {
			self.fRec8[l1 as usize] = 0.0;
		}
		for l2 in 0..3 {
			self.fRec7[l2 as usize] = 0.0;
		}
		for l3 in 0..3 {
			self.fRec6[l3 as usize] = 0.0;
		}
		for l4 in 0..3 {
			self.fRec5[l4 as usize] = 0.0;
		}
		for l5 in 0..3 {
			self.fRec4[l5 as usize] = 0.0;
		}
		for l6 in 0..3 {
			self.fRec3[l6 as usize] = 0.0;
		}
		for l7 in 0..3 {
			self.fRec2[l7 as usize] = 0.0;
		}
		for l8 in 0..3 {
			self.fRec1[l8 as usize] = 0.0;
		}
		for l9 in 0..3 {
			self.fRec0[l9 as usize] = 0.0;
		}
	}
	pub fn instance_constants(&mut self, sample_rate: i32) {
		// Obtaining locks on 0 static var(s)
		self.fSampleRate = sample_rate;
		self.fConst0 = 3.141592653589793 / F64::min(1.92e+05, F64::max(1.0, (self.fSampleRate) as F64));
	}
	pub fn instance_init(&mut self, sample_rate: i32) {
		self.instance_constants(sample_rate);
		self.instance_reset_params();
		self.instance_clear();
	}
	pub fn init(&mut self, sample_rate: i32) {
		mydsp::class_init(sample_rate);
		self.instance_init(sample_rate);
	}
	
	pub fn build_user_interface(&self, ui_interface: &mut dyn UI<FaustFloat>) {
		Self::build_user_interface_static(ui_interface);
	}
	
	pub fn build_user_interface_static(ui_interface: &mut dyn UI<FaustFloat>) {
		ui_interface.open_horizontal_box("Multi Band Filter");
		ui_interface.open_vertical_box("peak 0");
		ui_interface.declare(Some(ParamIndex(0)), "style", "knob");
		ui_interface.add_num_entry("Q factor", ParamIndex(0), 5e+01, 0.1, 1e+02, 0.1);
		ui_interface.declare(Some(ParamIndex(1)), "style", "knob");
		ui_interface.declare(Some(ParamIndex(1)), "unit", "Hz");
		ui_interface.add_num_entry("freq", ParamIndex(1), 1e+03, 2e+01, 2e+04, 1.0);
		ui_interface.declare(Some(ParamIndex(2)), "unit", "dB");
		ui_interface.add_vertical_slider("gain", ParamIndex(2), 0.0, -5e+01, 5e+01, 0.1);
		ui_interface.close_box();
		ui_interface.open_vertical_box("peak 1");
		ui_interface.declare(Some(ParamIndex(3)), "style", "knob");
		ui_interface.add_num_entry("Q factor", ParamIndex(3), 5e+01, 0.1, 1e+02, 0.1);
		ui_interface.declare(Some(ParamIndex(4)), "style", "knob");
		ui_interface.declare(Some(ParamIndex(4)), "unit", "Hz");
		ui_interface.add_num_entry("freq", ParamIndex(4), 2e+03, 2e+01, 2e+04, 1.0);
		ui_interface.declare(Some(ParamIndex(5)), "unit", "dB");
		ui_interface.add_vertical_slider("gain", ParamIndex(5), 0.0, -5e+01, 5e+01, 0.1);
		ui_interface.close_box();
		ui_interface.open_vertical_box("peak 2");
		ui_interface.declare(Some(ParamIndex(6)), "style", "knob");
		ui_interface.add_num_entry("Q factor", ParamIndex(6), 5e+01, 0.1, 1e+02, 0.1);
		ui_interface.declare(Some(ParamIndex(7)), "style", "knob");
		ui_interface.declare(Some(ParamIndex(7)), "unit", "Hz");
		ui_interface.add_num_entry("freq", ParamIndex(7), 3e+03, 2e+01, 2e+04, 1.0);
		ui_interface.declare(Some(ParamIndex(8)), "unit", "dB");
		ui_interface.add_vertical_slider("gain", ParamIndex(8), 0.0, -5e+01, 5e+01, 0.1);
		ui_interface.close_box();
		ui_interface.open_vertical_box("peak 3");
		ui_interface.declare(Some(ParamIndex(9)), "style", "knob");
		ui_interface.add_num_entry("Q factor", ParamIndex(9), 5e+01, 0.1, 1e+02, 0.1);
		ui_interface.declare(Some(ParamIndex(10)), "style", "knob");
		ui_interface.declare(Some(ParamIndex(10)), "unit", "Hz");
		ui_interface.add_num_entry("freq", ParamIndex(10), 4e+03, 2e+01, 2e+04, 1.0);
		ui_interface.declare(Some(ParamIndex(11)), "unit", "dB");
		ui_interface.add_vertical_slider("gain", ParamIndex(11), 0.0, -5e+01, 5e+01, 0.1);
		ui_interface.close_box();
		ui_interface.open_vertical_box("peak 4");
		ui_interface.declare(Some(ParamIndex(12)), "style", "knob");
		ui_interface.add_num_entry("Q factor", ParamIndex(12), 5e+01, 0.1, 1e+02, 0.1);
		ui_interface.declare(Some(ParamIndex(13)), "style", "knob");
		ui_interface.declare(Some(ParamIndex(13)), "unit", "Hz");
		ui_interface.add_num_entry("freq", ParamIndex(13), 5e+03, 2e+01, 2e+04, 1.0);
		ui_interface.declare(Some(ParamIndex(14)), "unit", "dB");
		ui_interface.add_vertical_slider("gain", ParamIndex(14), 0.0, -5e+01, 5e+01, 0.1);
		ui_interface.close_box();
		ui_interface.open_vertical_box("peak 5");
		ui_interface.declare(Some(ParamIndex(15)), "style", "knob");
		ui_interface.add_num_entry("Q factor", ParamIndex(15), 5e+01, 0.1, 1e+02, 0.1);
		ui_interface.declare(Some(ParamIndex(16)), "style", "knob");
		ui_interface.declare(Some(ParamIndex(16)), "unit", "Hz");
		ui_interface.add_num_entry("freq", ParamIndex(16), 6e+03, 2e+01, 2e+04, 1.0);
		ui_interface.declare(Some(ParamIndex(17)), "unit", "dB");
		ui_interface.add_vertical_slider("gain", ParamIndex(17), 0.0, -5e+01, 5e+01, 0.1);
		ui_interface.close_box();
		ui_interface.open_vertical_box("peak 6");
		ui_interface.declare(Some(ParamIndex(18)), "style", "knob");
		ui_interface.add_num_entry("Q factor", ParamIndex(18), 5e+01, 0.1, 1e+02, 0.1);
		ui_interface.declare(Some(ParamIndex(19)), "style", "knob");
		ui_interface.declare(Some(ParamIndex(19)), "unit", "Hz");
		ui_interface.add_num_entry("freq", ParamIndex(19), 7e+03, 2e+01, 2e+04, 1.0);
		ui_interface.declare(Some(ParamIndex(20)), "unit", "dB");
		ui_interface.add_vertical_slider("gain", ParamIndex(20), 0.0, -5e+01, 5e+01, 0.1);
		ui_interface.close_box();
		ui_interface.open_vertical_box("peak 7");
		ui_interface.declare(Some(ParamIndex(21)), "style", "knob");
		ui_interface.add_num_entry("Q factor", ParamIndex(21), 5e+01, 0.1, 1e+02, 0.1);
		ui_interface.declare(Some(ParamIndex(22)), "style", "knob");
		ui_interface.declare(Some(ParamIndex(22)), "unit", "Hz");
		ui_interface.add_num_entry("freq", ParamIndex(22), 8e+03, 2e+01, 2e+04, 1.0);
		ui_interface.declare(Some(ParamIndex(23)), "unit", "dB");
		ui_interface.add_vertical_slider("gain", ParamIndex(23), 0.0, -5e+01, 5e+01, 0.1);
		ui_interface.close_box();
		ui_interface.open_vertical_box("peak 8");
		ui_interface.declare(Some(ParamIndex(24)), "style", "knob");
		ui_interface.add_num_entry("Q factor", ParamIndex(24), 5e+01, 0.1, 1e+02, 0.1);
		ui_interface.declare(Some(ParamIndex(25)), "style", "knob");
		ui_interface.declare(Some(ParamIndex(25)), "unit", "Hz");
		ui_interface.add_num_entry("freq", ParamIndex(25), 9e+03, 2e+01, 2e+04, 1.0);
		ui_interface.declare(Some(ParamIndex(26)), "unit", "dB");
		ui_interface.add_vertical_slider("gain", ParamIndex(26), 0.0, -5e+01, 5e+01, 0.1);
		ui_interface.close_box();
		ui_interface.open_vertical_box("peak 9");
		ui_interface.declare(Some(ParamIndex(27)), "style", "knob");
		ui_interface.add_num_entry("Q factor", ParamIndex(27), 5e+01, 0.1, 1e+02, 0.1);
		ui_interface.declare(Some(ParamIndex(28)), "style", "knob");
		ui_interface.declare(Some(ParamIndex(28)), "unit", "Hz");
		ui_interface.add_num_entry("freq", ParamIndex(28), 1e+04, 2e+01, 2e+04, 1.0);
		ui_interface.declare(Some(ParamIndex(29)), "unit", "dB");
		ui_interface.add_vertical_slider("gain", ParamIndex(29), 0.0, -5e+01, 5e+01, 0.1);
		ui_interface.close_box();
		ui_interface.close_box();
	}
	
	pub fn get_param(&self, param: ParamIndex) -> Option<FaustFloat> {
		match param.0 {
			28 => Some(self.fEntry0),
			27 => Some(self.fEntry1),
			13 => Some(self.fEntry10),
			12 => Some(self.fEntry11),
			10 => Some(self.fEntry12),
			9 => Some(self.fEntry13),
			7 => Some(self.fEntry14),
			6 => Some(self.fEntry15),
			4 => Some(self.fEntry16),
			3 => Some(self.fEntry17),
			1 => Some(self.fEntry18),
			0 => Some(self.fEntry19),
			25 => Some(self.fEntry2),
			24 => Some(self.fEntry3),
			22 => Some(self.fEntry4),
			21 => Some(self.fEntry5),
			19 => Some(self.fEntry6),
			18 => Some(self.fEntry7),
			16 => Some(self.fEntry8),
			15 => Some(self.fEntry9),
			29 => Some(self.fVslider0),
			26 => Some(self.fVslider1),
			23 => Some(self.fVslider2),
			20 => Some(self.fVslider3),
			17 => Some(self.fVslider4),
			14 => Some(self.fVslider5),
			11 => Some(self.fVslider6),
			8 => Some(self.fVslider7),
			5 => Some(self.fVslider8),
			2 => Some(self.fVslider9),
			_ => None,
		}
	}
	
	pub fn set_param(&mut self, param: ParamIndex, value: FaustFloat) {
		match param.0 {
			28 => { self.fEntry0 = value }
			27 => { self.fEntry1 = value }
			13 => { self.fEntry10 = value }
			12 => { self.fEntry11 = value }
			10 => { self.fEntry12 = value }
			9 => { self.fEntry13 = value }
			7 => { self.fEntry14 = value }
			6 => { self.fEntry15 = value }
			4 => { self.fEntry16 = value }
			3 => { self.fEntry17 = value }
			1 => { self.fEntry18 = value }
			0 => { self.fEntry19 = value }
			25 => { self.fEntry2 = value }
			24 => { self.fEntry3 = value }
			22 => { self.fEntry4 = value }
			21 => { self.fEntry5 = value }
			19 => { self.fEntry6 = value }
			18 => { self.fEntry7 = value }
			16 => { self.fEntry8 = value }
			15 => { self.fEntry9 = value }
			29 => { self.fVslider0 = value }
			26 => { self.fVslider1 = value }
			23 => { self.fVslider2 = value }
			20 => { self.fVslider3 = value }
			17 => { self.fVslider4 = value }
			14 => { self.fVslider5 = value }
			11 => { self.fVslider6 = value }
			8 => { self.fVslider7 = value }
			5 => { self.fVslider8 = value }
			2 => { self.fVslider9 = value }
			_ => {}
		}
	}

	pub fn compute(
		&mut self,
		count: usize,
		inputs: &[impl AsRef<[FaustFloat]>],
		outputs: &mut[impl AsMut<[FaustFloat]>],
	) {

		// Obtaining locks on 0 static var(s)
		let [inputs0, .. ] = inputs.as_ref() else { panic!("wrong number of input buffers"); };
		let inputs0 = inputs0.as_ref()[..count].iter();
		let [outputs0, .. ] = outputs.as_mut() else { panic!("wrong number of output buffers"); };
		let outputs0 = outputs0.as_mut()[..count].iter_mut();

		let mut fSlow0: F64 = F64::tan(self.fConst0 * (self.fEntry0) as F64);
		let mut fSlow1: F64 = (self.fEntry1) as F64;
		let mut fSlow2: F64 = F64::powf(1e+01, -(0.05 * (self.fVslider0) as F64)) / fSlow1;
		let mut fSlow3: F64 = 1.0 / (fSlow0 * (fSlow0 + fSlow2) + 1.0);
		let mut fSlow4: F64 = 2.0 * (mydsp_faustpower2_f(fSlow0) + -1.0);
		let mut fSlow5: F64 = F64::tan(self.fConst0 * (self.fEntry2) as F64);
		let mut fSlow6: F64 = (self.fEntry3) as F64;
		let mut fSlow7: F64 = F64::powf(1e+01, -(0.05 * (self.fVslider1) as F64)) / fSlow6;
		let mut fSlow8: F64 = 1.0 / (fSlow5 * (fSlow5 + fSlow7) + 1.0);
		let mut fSlow9: F64 = 2.0 * (mydsp_faustpower2_f(fSlow5) + -1.0);
		let mut fSlow10: F64 = F64::tan(self.fConst0 * (self.fEntry4) as F64);
		let mut fSlow11: F64 = (self.fEntry5) as F64;
		let mut fSlow12: F64 = F64::powf(1e+01, -(0.05 * (self.fVslider2) as F64)) / fSlow11;
		let mut fSlow13: F64 = 1.0 / (fSlow10 * (fSlow10 + fSlow12) + 1.0);
		let mut fSlow14: F64 = 2.0 * (mydsp_faustpower2_f(fSlow10) + -1.0);
		let mut fSlow15: F64 = F64::tan(self.fConst0 * (self.fEntry6) as F64);
		let mut fSlow16: F64 = (self.fEntry7) as F64;
		let mut fSlow17: F64 = F64::powf(1e+01, -(0.05 * (self.fVslider3) as F64)) / fSlow16;
		let mut fSlow18: F64 = 1.0 / (fSlow15 * (fSlow15 + fSlow17) + 1.0);
		let mut fSlow19: F64 = 2.0 * (mydsp_faustpower2_f(fSlow15) + -1.0);
		let mut fSlow20: F64 = F64::tan(self.fConst0 * (self.fEntry8) as F64);
		let mut fSlow21: F64 = (self.fEntry9) as F64;
		let mut fSlow22: F64 = F64::powf(1e+01, -(0.05 * (self.fVslider4) as F64)) / fSlow21;
		let mut fSlow23: F64 = 1.0 / (fSlow20 * (fSlow20 + fSlow22) + 1.0);
		let mut fSlow24: F64 = 2.0 * (mydsp_faustpower2_f(fSlow20) + -1.0);
		let mut fSlow25: F64 = F64::tan(self.fConst0 * (self.fEntry10) as F64);
		let mut fSlow26: F64 = (self.fEntry11) as F64;
		let mut fSlow27: F64 = F64::powf(1e+01, -(0.05 * (self.fVslider5) as F64)) / fSlow26;
		let mut fSlow28: F64 = 1.0 / (fSlow25 * (fSlow25 + fSlow27) + 1.0);
		let mut fSlow29: F64 = 2.0 * (mydsp_faustpower2_f(fSlow25) + -1.0);
		let mut fSlow30: F64 = F64::tan(self.fConst0 * (self.fEntry12) as F64);
		let mut fSlow31: F64 = (self.fEntry13) as F64;
		let mut fSlow32: F64 = F64::powf(1e+01, -(0.05 * (self.fVslider6) as F64)) / fSlow31;
		let mut fSlow33: F64 = 1.0 / (fSlow30 * (fSlow30 + fSlow32) + 1.0);
		let mut fSlow34: F64 = 2.0 * (mydsp_faustpower2_f(fSlow30) + -1.0);
		let mut fSlow35: F64 = F64::tan(self.fConst0 * (self.fEntry14) as F64);
		let mut fSlow36: F64 = (self.fEntry15) as F64;
		let mut fSlow37: F64 = F64::powf(1e+01, -(0.05 * (self.fVslider7) as F64)) / fSlow36;
		let mut fSlow38: F64 = 1.0 / (fSlow35 * (fSlow35 + fSlow37) + 1.0);
		let mut fSlow39: F64 = 2.0 * (mydsp_faustpower2_f(fSlow35) + -1.0);
		let mut fSlow40: F64 = F64::tan(self.fConst0 * (self.fEntry16) as F64);
		let mut fSlow41: F64 = (self.fEntry17) as F64;
		let mut fSlow42: F64 = F64::powf(1e+01, -(0.05 * (self.fVslider8) as F64)) / fSlow41;
		let mut fSlow43: F64 = 1.0 / (fSlow40 * (fSlow40 + fSlow42) + 1.0);
		let mut fSlow44: F64 = 2.0 * (mydsp_faustpower2_f(fSlow40) + -1.0);
		let mut fSlow45: F64 = F64::tan(self.fConst0 * (self.fEntry18) as F64);
		let mut fSlow46: F64 = (self.fEntry19) as F64;
		let mut fSlow47: F64 = F64::powf(1e+01, -(0.05 * (self.fVslider9) as F64)) / fSlow46;
		let mut fSlow48: F64 = 1.0 / (fSlow45 * (fSlow45 + fSlow47) + 1.0);
		let mut fSlow49: F64 = 2.0 * (mydsp_faustpower2_f(fSlow45) + -1.0);
		let mut fSlow50: F64 = fSlow45 * (fSlow45 - fSlow47) + 1.0;
		let mut fSlow51: F64 = 1.0 / fSlow46;
		let mut fSlow52: F64 = fSlow45 * (fSlow45 + fSlow51) + 1.0;
		let mut fSlow53: F64 = 1.0 - fSlow45 * (fSlow51 - fSlow45);
		let mut fSlow54: F64 = fSlow40 * (fSlow40 - fSlow42) + 1.0;
		let mut fSlow55: F64 = 1.0 / fSlow41;
		let mut fSlow56: F64 = fSlow40 * (fSlow40 + fSlow55) + 1.0;
		let mut fSlow57: F64 = 1.0 - fSlow40 * (fSlow55 - fSlow40);
		let mut fSlow58: F64 = fSlow35 * (fSlow35 - fSlow37) + 1.0;
		let mut fSlow59: F64 = 1.0 / fSlow36;
		let mut fSlow60: F64 = fSlow35 * (fSlow35 + fSlow59) + 1.0;
		let mut fSlow61: F64 = 1.0 - fSlow35 * (fSlow59 - fSlow35);
		let mut fSlow62: F64 = fSlow30 * (fSlow30 - fSlow32) + 1.0;
		let mut fSlow63: F64 = 1.0 / fSlow31;
		let mut fSlow64: F64 = fSlow30 * (fSlow30 + fSlow63) + 1.0;
		let mut fSlow65: F64 = 1.0 - fSlow30 * (fSlow63 - fSlow30);
		let mut fSlow66: F64 = fSlow25 * (fSlow25 - fSlow27) + 1.0;
		let mut fSlow67: F64 = 1.0 / fSlow26;
		let mut fSlow68: F64 = fSlow25 * (fSlow25 + fSlow67) + 1.0;
		let mut fSlow69: F64 = 1.0 - fSlow25 * (fSlow67 - fSlow25);
		let mut fSlow70: F64 = fSlow20 * (fSlow20 - fSlow22) + 1.0;
		let mut fSlow71: F64 = 1.0 / fSlow21;
		let mut fSlow72: F64 = fSlow20 * (fSlow20 + fSlow71) + 1.0;
		let mut fSlow73: F64 = 1.0 - fSlow20 * (fSlow71 - fSlow20);
		let mut fSlow74: F64 = fSlow15 * (fSlow15 - fSlow17) + 1.0;
		let mut fSlow75: F64 = 1.0 / fSlow16;
		let mut fSlow76: F64 = fSlow15 * (fSlow15 + fSlow75) + 1.0;
		let mut fSlow77: F64 = 1.0 - fSlow15 * (fSlow75 - fSlow15);
		let mut fSlow78: F64 = fSlow10 * (fSlow10 - fSlow12) + 1.0;
		let mut fSlow79: F64 = 1.0 / fSlow11;
		let mut fSlow80: F64 = fSlow10 * (fSlow10 + fSlow79) + 1.0;
		let mut fSlow81: F64 = 1.0 - fSlow10 * (fSlow79 - fSlow10);
		let mut fSlow82: F64 = fSlow5 * (fSlow5 - fSlow7) + 1.0;
		let mut fSlow83: F64 = 1.0 / fSlow6;
		let mut fSlow84: F64 = fSlow5 * (fSlow5 + fSlow83) + 1.0;
		let mut fSlow85: F64 = 1.0 - fSlow5 * (fSlow83 - fSlow5);
		let mut fSlow86: F64 = fSlow0 * (fSlow0 - fSlow2) + 1.0;
		let mut fSlow87: F64 = 1.0 / fSlow1;
		let mut fSlow88: F64 = fSlow0 * (fSlow0 + fSlow87) + 1.0;
		let mut fSlow89: F64 = 1.0 - fSlow0 * (fSlow87 - fSlow0);
		let zipped_iterators = inputs0.zip(outputs0);

		for (input0, output0) in zipped_iterators {
			let mut fTemp0: F64 = fSlow49 * self.fRec9[1];
			self.fRec9[0] = (*input0) as F64 - fSlow48 * (fSlow50 * self.fRec9[2] + fTemp0);
			let mut fTemp1: F64 = fSlow44 * self.fRec8[1];
			self.fRec8[0] = fSlow48 * (fTemp0 + fSlow52 * self.fRec9[0] + fSlow53 * self.fRec9[2]) - fSlow43 * (fSlow54 * self.fRec8[2] + fTemp1);
			let mut fTemp2: F64 = fSlow39 * self.fRec7[1];
			self.fRec7[0] = fSlow43 * (fTemp1 + fSlow56 * self.fRec8[0] + fSlow57 * self.fRec8[2]) - fSlow38 * (fSlow58 * self.fRec7[2] + fTemp2);
			let mut fTemp3: F64 = fSlow34 * self.fRec6[1];
			self.fRec6[0] = fSlow38 * (fTemp2 + fSlow60 * self.fRec7[0] + fSlow61 * self.fRec7[2]) - fSlow33 * (fSlow62 * self.fRec6[2] + fTemp3);
			let mut fTemp4: F64 = fSlow29 * self.fRec5[1];
			self.fRec5[0] = fSlow33 * (fTemp3 + fSlow64 * self.fRec6[0] + fSlow65 * self.fRec6[2]) - fSlow28 * (fSlow66 * self.fRec5[2] + fTemp4);
			let mut fTemp5: F64 = fSlow24 * self.fRec4[1];
			self.fRec4[0] = fSlow28 * (fTemp4 + fSlow68 * self.fRec5[0] + fSlow69 * self.fRec5[2]) - fSlow23 * (fSlow70 * self.fRec4[2] + fTemp5);
			let mut fTemp6: F64 = fSlow19 * self.fRec3[1];
			self.fRec3[0] = fSlow23 * (fTemp5 + fSlow72 * self.fRec4[0] + fSlow73 * self.fRec4[2]) - fSlow18 * (fSlow74 * self.fRec3[2] + fTemp6);
			let mut fTemp7: F64 = fSlow14 * self.fRec2[1];
			self.fRec2[0] = fSlow18 * (fTemp6 + fSlow76 * self.fRec3[0] + fSlow77 * self.fRec3[2]) - fSlow13 * (fSlow78 * self.fRec2[2] + fTemp7);
			let mut fTemp8: F64 = fSlow9 * self.fRec1[1];
			self.fRec1[0] = fSlow13 * (fTemp7 + fSlow80 * self.fRec2[0] + fSlow81 * self.fRec2[2]) - fSlow8 * (fSlow82 * self.fRec1[2] + fTemp8);
			let mut fTemp9: F64 = fSlow4 * self.fRec0[1];
			self.fRec0[0] = fSlow8 * (fTemp8 + fSlow84 * self.fRec1[0] + fSlow85 * self.fRec1[2]) - fSlow3 * (fSlow86 * self.fRec0[2] + fTemp9);
			*output0 = (fSlow3 * (fTemp9 + fSlow88 * self.fRec0[0] + fSlow89 * self.fRec0[2])) as FaustFloat;
			self.fRec9[2] = self.fRec9[1];
			self.fRec9[1] = self.fRec9[0];
			self.fRec8[2] = self.fRec8[1];
			self.fRec8[1] = self.fRec8[0];
			self.fRec7[2] = self.fRec7[1];
			self.fRec7[1] = self.fRec7[0];
			self.fRec6[2] = self.fRec6[1];
			self.fRec6[1] = self.fRec6[0];
			self.fRec5[2] = self.fRec5[1];
			self.fRec5[1] = self.fRec5[0];
			self.fRec4[2] = self.fRec4[1];
			self.fRec4[1] = self.fRec4[0];
			self.fRec3[2] = self.fRec3[1];
			self.fRec3[1] = self.fRec3[0];
			self.fRec2[2] = self.fRec2[1];
			self.fRec2[1] = self.fRec2[0];
			self.fRec1[2] = self.fRec1[1];
			self.fRec1[1] = self.fRec1[0];
			self.fRec0[2] = self.fRec0[1];
			self.fRec0[1] = self.fRec0[0];
		}
		
	}
}

#[cfg(not(target_arch = "wasm32"))] // Compile ffi bindings only on non-wasm targets
	mod ffi {
	use core::ffi::c_double;
	// Conditionally compile the link attribute only on non-Windows platforms
	#[cfg_attr(not(target_os="windows"), link(name="m"))]
	unsafe extern "C" {
		pub fn remainder(from: c_double, to: c_double) -> c_double;
		pub fn rint(val: c_double) -> c_double;
	}
}
fn remainder(from: f64, to: f64) -> f64 {
	#[cfg(not(target_arch = "wasm32"))] // non-wasm targets use ffi bindings
	unsafe { ffi::remainder(from, to) }
	#[cfg(target_arch = "wasm32")] // wasm relies on libm
	libm::remainder(from, to)
}
fn rint(val: f64) -> f64 {
	#[cfg(not(target_arch = "wasm32"))] // non-wasm targets use ffi bindings
	unsafe { ffi::rint(val) }
	#[cfg(target_arch = "wasm32")] // wasm relies on libm
	libm::rint(val)
}

impl FaustDsp for mydsp {
	type T = FaustFloat;
	fn new() -> Self where Self: Sized {
		Self::new()
	}
	fn metadata(&self, m: &mut dyn Meta) {
		self.metadata(m)
	}
	fn get_sample_rate(&self) -> i32 {
		self.get_sample_rate()
	}
	fn get_num_inputs(&self) -> i32 {
		FAUST_INPUTS as i32
	}
	fn get_num_outputs(&self) -> i32 {
		FAUST_OUTPUTS as i32
	}
	fn class_init(sample_rate: i32) where Self: Sized {
		Self::class_init(sample_rate);
	}
	fn instance_reset_params(&mut self) {
		self.instance_reset_params()
	}
	fn instance_clear(&mut self) {
		self.instance_clear()
	}
	fn instance_constants(&mut self, sample_rate: i32) {
		self.instance_constants(sample_rate)
	}
	fn instance_init(&mut self, sample_rate: i32) {
		self.instance_init(sample_rate)
	}
	fn init(&mut self, sample_rate: i32) {
		self.init(sample_rate)
	}
	fn build_user_interface(&self, ui_interface: &mut dyn UI<Self::T>) {
		self.build_user_interface(ui_interface)
	}
	fn build_user_interface_static(ui_interface: &mut dyn UI<Self::T>) where Self: Sized {
		Self::build_user_interface_static(ui_interface);
	}
	fn get_param(&self, param: ParamIndex) -> Option<Self::T> {
		self.get_param(param)
	}
	fn set_param(&mut self, param: ParamIndex, value: Self::T) {
		self.set_param(param, value)
	}
	fn compute(&mut self, count: i32, inputs: &[&[Self::T]], outputs: &mut [&mut [Self::T]]) {
		self.compute(count as usize, inputs, outputs)
	}
}
